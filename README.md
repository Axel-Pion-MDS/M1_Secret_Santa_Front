These are the commands to execute in order to access the project

- `docker-compose build`

- `docker run -p 3000:3000 -d dockerized-react`

Access the project at this address 

- `http://localhost:3000/`